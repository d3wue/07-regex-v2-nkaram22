def find_p(n):
    p = []
    for num in range(2, n + 1):
        for i in range(2, num):
            if num % i == 0:
                break
            else:
                p.append(num)
                return p

upper_limit = 10
p_numbers = find_p(upper_limit)
print(p_numbers)